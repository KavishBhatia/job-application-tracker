# Get a free-tier key at https://ai.google.dev/ -> "Get API key"
# Optional: the app works fully without it, except free-text auto-parsing
# will fall back to manual entry (see quickstart.md).
GEMINI_API_KEY=your-key-here
